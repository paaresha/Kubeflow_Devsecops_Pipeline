🟢 Entry Level (Q1–Q10) — Highlights
Q1	ArgoCD Synced ≠ deployed — checking git push failure from CI bot
Q2	Why OIDC > static AWS keys — explain the id-token: write + role-to-assume mechanics
Q3	Kyverno rejecting :latest tag — reading admission error, fixing the image tag
Q4	Dissecting the HighErrorRate PromQL expression line-by-line
Q5	Helm -f values.yaml -f values-prod.yaml layering pattern
Q6	Why Terraform apply only on main, never on PR branches
Q7	Debugging secret "db-credentials" not found via ESO + IRSA
Q8	Trivy exit-code: "0" — what it means and how to harden it to block deploys
Q9	First 5 kubectl commands on a CrashLoopBackOff pod
Q10	Smoke test fails → kubectl rollout undo auto-rollback flow

🟡 Mid Level (Q11–Q20) — Highlights
Q11	Full E2E trace: push → CI → ECR → yq → git commit → ArgoCD sync → K8s rolling update
Q12	Race condition between 2 simultaneous pushes and the git push step in CI
Q13	IRSA trust chain: ServiceAccount JWT → OIDC → AssumeRoleWithWebIdentity → Secrets Manager
Q14	Kyverno Audit vs Enforce — production rollout strategy
Q15	Fix NoOrdersReceived firing at 2am using Alertmanager time_intervals
Q16	Recovering a Helm release stuck in pending-upgrade state
Q17	ArgoCD selfHeal: true — what happens when someone kubectl deletes a deployment
Q18	SonarQube Quality Gate below 70% — PM wants to bypass, what do you do
Q19	HPAMaxedOut alert: why Cluster Autoscaler isn't adding nodes despite full replicas
Q20	10-minute production incident response walkthrough (ServiceDown + HighErrorRate firing)

🔴 Senior Level (Q21–Q30) — Highlights
Q21	Monorepo scale problems at 50+ services → multi-repo GitOps architecture
Q22	Hardening the OIDC trust policy wildcard repo:ORG/*:* → per-branch per-repo
Q23	Terraform state corruption with simultaneous apply + DynamoDB locking
Q24	3-minute outage that triggered zero alerts — finding the for: 5m gap, SLO fix
Q25	Designing a proper dev → staging → prod promotion pipeline with canary rollouts
Q26	Kyverno mutation policy to auto-inject a log forwarder sidecar
Q27	Pod can reach K8s services but not RDS — VPC CNI / Security Group deep dive
Q28	Supply chain security gaps: Cosign signing, SLSA provenance, digest pinning
Q29	Onboarding a new payment-service end-to-end — every file to create, in order
Q30	Architecture trade-off: this 5-tool stack vs AWS managed alternatives (App Runner, etc.)
